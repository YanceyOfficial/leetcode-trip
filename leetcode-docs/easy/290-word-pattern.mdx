---
id: 290-word-pattern
title: 单词规律
sidebar_label: 290. 单词规律
keywords:
  - HashMap
---

:::success Tips
题目类型: HashMap

相关题目:

- [205. 同构字符串](/leetcode/easy/205-is-isomorphic)

:::

## 题目

给定一种规律 `pattern` 和一个字符串 `s`, 判断 `s` 是否遵循相同的规律.

这里的**遵循**指完全匹配, 例如, `pattern` 里的每个字母和字符串 `s` 中的每个非空单词之间存在着双向连接的对应规律.

:::note 提示:

- `1 <= pattern.length <= 300`
- `pattern` 只包含小写英文字母
- `1 <= s.length <= 3000`
- `s` 只包含小写英文字母和 `' '`
- `s` **不包含**任何前导或尾随对空格
- `s` 中每个单词都被**单个空格**分隔

:::

:::info 示例

```bash
输入: pattern = "abba", s = "dog cat cat dog"
输出: true
```

```bash
输入:pattern = "abba", s = "dog cat cat fish"
输出: false
```

```bash
输入: pattern = "aaaa", s = "dog cat cat dog"
输出: false
```

:::

## 题解

```ts
/**
 * @param {string} pattern
 * @param {string} str
 * @return {boolean}
 */
var wordPattern = function (pattern, str) {
  const words = s.split(' ')
  if (pattern.length !== words.length) return false

  const p2W = {}
  const w2P = {}
  for (let i = 0; i < pattern.length; i++) {
    if (p2W[pattern[i]] && p2W[pattern[i]] !== words[i]) return false
    if (w2P[words[i]] && w2P[words[i]] !== pattern[i]) return false

    p2W[pattern[i]] = words[i]
    w2P[words[i]] = pattern[i]
  }

  return true
}
```
